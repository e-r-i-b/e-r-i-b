// нопка. действие которой выполн€етс€ по умолчанию (по Enter)
var defaultClientButton;

function findDefaultClientButton() {
    return defaultClientButton;
}

function setDefaultClientButon(button){
    defaultClientButton = button;
}
