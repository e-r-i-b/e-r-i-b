<?xml version="1.0" encoding="windows-1251"?>

<bankroll-registry xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:noNamespaceSchemaLocation="../../../schemas/bankroll-registry.xsd">

    <account-config>
        <in-use>true</in-use>

        <!-- Время жизни списка счетов клиента в секундах (12 часов) -->
        <list-lifetime>43200</list-lifetime>
    </account-config>


    <card-config>
        <in-use>true</in-use>

        <!-- Время жизни списка карт клиента в секундах (12 часов) -->
        <list-lifetime>43200</list-lifetime>
    </card-config>

    
    <loan-config>
        <in-use>false</in-use>

        <!-- Время жизни списка кредитов клиента в секундах (12 часов) -->
        <list-lifetime>43200</list-lifetime>
    </loan-config>

</bankroll-registry>
